[package]
name = "yata"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
reqwest = { version = "0.13.4", features = ["blocking", "gzip"] }
unicode-width = "0.2"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "parse"
harness = false
